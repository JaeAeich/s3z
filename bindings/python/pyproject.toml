[build-system]
build-backend = "maturin"
requires = ["maturin>=1.8"]

[project]
name = "s3z"
readme = "README.md"
requires-python = ">=3.11"
version = "0.1.0-rc.2"

[tool.maturin]
module-name = "s3z"
